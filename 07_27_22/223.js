// const add = function(x,y) {
//     return x + y;
// }

const add = (x, y) => {
    return x + y
}

const square = (x) => {
    return x ** 2
}

const rollDie = () => {
    return Math.floor(Math.random() * 6) + 1;
}

const greet = (name) => {
    return `Hey ${name}!`
}