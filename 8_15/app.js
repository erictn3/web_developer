console.log('sending request to server')
setTimeout(() => {
    console.log(
        'data from server'
    )
}, 3000);

console.log('this sends first');

// browser does the work with their own browser apis